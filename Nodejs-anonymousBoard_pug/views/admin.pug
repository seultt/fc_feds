doctype html
html
  head
    meta(charset='utf-8')
    title 익명게시판
    link(type='text/css' rel='stylesheet' href='/static/layout.css')
    link(type='text/css' rel='stylesheet' href='/static/admin.css')
  body
    .box
      header.header.clearfix
        .wrapper
          h1.logo 
            a(href="/admin") Node.js admin
          .right
            a.login(href="/admin") Login
            a.user(href="#") User
      section.wrapper.board 
        .board-top.clearfix
          h2.board-title 익명게시판
          a.board-add(href="/addPost") +ADD POST
        .board-list.clearfix
          each item in data
            a.list(href="/content/"+item.num)
              - if(item.num<10)
                  .list-num= '0' + item.num
              - else
                  .list-num= item.num 
              .list-content 
                p.content-title= item.title
                p.content-text= item.content
              .list-etc
                p.center
                  span.etc-date= item.date
                  span.etc-user= item.user+' 님'
            form.del(method="post" action="/admin/${item.num}/del")
              button(type="submit")
                img(src="/static/icon_del.svg")
      footer.footer
        .wrapper
          h2.logo Node.js
          p.left IE9 이상에 최적화된 사이트 입니다.        